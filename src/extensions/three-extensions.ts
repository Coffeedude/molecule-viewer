import { extend } from '@react-three/fiber';
import { LineSegments, LineBasicMaterial, SphereGeometry, MeshStandardMaterial } from 'three';

extend({
  LineSegments,
  LineBasicMaterial,
  SphereGeometry,
  MeshStandardMaterial,
});
